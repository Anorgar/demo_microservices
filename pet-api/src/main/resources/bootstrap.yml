spring:
  application:
    name: pet-api
  thymeleaf:
    prefix: "classpath:/templates/"

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic


